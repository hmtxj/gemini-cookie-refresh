name: 刷新 Gemini Business Cookie（Windows）

on:
  # 手动触发
  workflow_dispatch:

jobs:
  refresh:
    runs-on: windows-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 安装 Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable
      
      - name: 安装依赖
        run: |
          pip install DrissionPage requests huggingface_hub psycopg2-binary pyyaml
          chrome --version 2>$null || echo "Chrome installed via action"
      
      # 从 Secret 恢复 Clash 配置并启动代理
      - name: 设置代理
        env:
          CLASH_CONFIG: ${{ secrets.CLASH_CONFIG }}
        shell: pwsh
        run: |
          Write-Host "📦 下载 mihomo (Clash Meta) for Windows..."
          
          # 下载 Windows 版本的 mihomo
          $url = "https://github.com/MetaCubeX/mihomo/releases/download/v1.18.10/mihomo-windows-amd64-v1.18.10.zip"
          $mirrorUrl = "https://mirror.ghproxy.com/$url"
          
          try {
              Invoke-WebRequest -Uri $url -OutFile mihomo.zip -TimeoutSec 60
          } catch {
              Write-Host "使用镜像下载..."
              Invoke-WebRequest -Uri $mirrorUrl -OutFile mihomo.zip -TimeoutSec 60
          }
          
          Expand-Archive -Path mihomo.zip -DestinationPath . -Force
          Move-Item -Path "mihomo-windows-amd64.exe" -Destination "mihomo.exe" -Force
          
          Write-Host "📝 从 Secret 恢复 Clash 配置..."
          $env:CLASH_CONFIG | Out-File -FilePath local.yaml -Encoding utf8 -NoNewline
          
          Write-Host "📝 配置文件内容（脱敏）:"
          Get-Content local.yaml | Select-Object -First 30 | ForEach-Object {
              $_ -replace 'password:.*', 'password: ***' -replace 'uuid:.*', 'uuid: ***'
          }
          
          Write-Host ""
          Write-Host "📝 修改配置..."
          python update_clash_config.py local.yaml
          
          Write-Host ""
          Write-Host "🚀 启动代理..."
          Start-Process -FilePath ".\mihomo.exe" -ArgumentList "-f", "local.yaml" -RedirectStandardOutput "clash.log" -RedirectStandardError "clash_err.log" -NoNewWindow
          Start-Sleep -Seconds 10
          
          Write-Host ""
          Write-Host "📋 Clash 启动日志:"
          if (Test-Path clash.log) { Get-Content clash.log }
          if (Test-Path clash_err.log) { Get-Content clash_err.log }
          
          Write-Host ""
          Write-Host "🔍 验证代理..."
          Write-Host "📍 GitHub Actions 原始 IP (不走代理):"
          try {
              (Invoke-WebRequest -Uri "https://api.ipify.org" -TimeoutSec 10).Content
          } catch {
              Write-Host "获取 IP 失败"
          }
          
          Write-Host ""
          Write-Host "📍 通过代理的 IP (代理节点):"
          try {
              (Invoke-WebRequest -Uri "https://api.ipify.org" -Proxy "http://127.0.0.1:7890" -TimeoutSec 15).Content
          } catch {
              Write-Host "代理连接失败: $_"
          }
          
          Write-Host ""
          Write-Host "📍 通过代理访问 Google:"
          try {
              $response = Invoke-WebRequest -Uri "https://www.google.com" -Proxy "http://127.0.0.1:7890" -TimeoutSec 15
              Write-Host "HTTP $($response.StatusCode)"
          } catch {
              Write-Host "失败: $_"
          }
      
      - name: 从 Secret 恢复 accounts.json
        shell: pwsh
        env:
          ACCOUNTS_JSON: ${{ secrets.ACCOUNTS_JSON }}
        run: |
          $env:ACCOUNTS_JSON | Out-File -FilePath accounts.json -Encoding utf8 -NoNewline
          $size = (Get-Item accounts.json).Length
          Write-Host "accounts.json 内容长度: $size 字节"
      
      - name: 运行刷新脚本
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_ID: ${{ secrets.HF_SPACE_ID }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PROXY_URL: http://127.0.0.1:7890
        shell: pwsh
        run: |
          # 设置编码为 UTF-8
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          $env:PYTHONIOENCODING = "utf-8"
          
          # 运行脚本
          python refresh_accounts.py --force
      
      - name: 上传调试截图
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-windows
          path: screenshots/
          if-no-files-found: ignore
      
      - name: 更新 Secret 中的 accounts.json
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        shell: pwsh
        run: |
          Get-Content accounts.json | gh secret set ACCOUNTS_JSON

name: 刷新 Gemini Business Cookie

on:
  # 定时执行：每 10 小时运行一次
  schedule:
    - cron: '0 */10 * * *'
  # 手动触发
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 安装依赖
        run: |
          pip install playwright requests huggingface_hub psycopg2-binary pyyaml
          playwright install chromium
      
      # 下载并启动代理
      - name: 设置代理
        env:
          SUBSCRIBE_URL: ${{ secrets.SUBSCRIBE_URL }}
        run: |
          echo "📦 下载 mihomo (Clash Meta)..."
          wget -q https://github.com/MetaCubeX/mihomo/releases/download/v1.18.10/mihomo-linux-amd64-v1.18.10.gz -O mihomo.gz || \
          wget -q https://mirror.ghproxy.com/https://github.com/MetaCubeX/mihomo/releases/download/v1.18.10/mihomo-linux-amd64-v1.18.10.gz -O mihomo.gz
          gunzip mihomo.gz
          chmod +x mihomo
          
          echo "📥 下载订阅配置..."
          curl -sL -o config.yaml "$SUBSCRIBE_URL"
          
          echo "📝 修改配置..."
          python3 update_clash_config.py config.yaml
          
          echo "🚀 启动代理..."
          nohup ./mihomo -f config.yaml > clash.log 2>&1 &
          sleep 5
          
          echo "🔍 验证代理..."
          if curl -s --proxy http://127.0.0.1:7890 --connect-timeout 10 https://www.google.com > /dev/null; then
            echo "✅ 代理可用"
          else
            echo "⚠️ 代理验证失败，继续运行（可能是网络问题）"
            cat clash.log || true
          fi
      
      - name: 从 Secret 恢复 accounts.json
        env:
          ACCOUNTS_JSON: ${{ secrets.ACCOUNTS_JSON }}
        run: |
          printf '%s' "$ACCOUNTS_JSON" > accounts.json
          echo "accounts.json 内容长度: $(wc -c < accounts.json) 字节"
      
      - name: 运行刷新脚本
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_ID: ${{ secrets.HF_SPACE_ID }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PROXY_URL: http://127.0.0.1:7890
        run: |
          python refresh_accounts.py --force
      
      - name: 上传调试截图
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: screenshots/
          if-no-files-found: ignore
      
      - name: 更新 Secret 中的 accounts.json
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          gh secret set ACCOUNTS_JSON < accounts.json
